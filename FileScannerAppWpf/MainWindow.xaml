<Window x:Class="FileScannerApp.Wpf.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="File Scanner"
        Height="820"
        Width="1400"
        MinHeight="720"
        MinWidth="1160"
        WindowStartupLocation="CenterScreen">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <Border Grid.Row="0" Padding="24" CornerRadius="18" Background="#FF0F172A">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>

                <StackPanel>
                    <TextBlock Foreground="White" FontSize="28" FontWeight="Bold" Text="File Scanner" />
                    <TextBlock Margin="0,8,0,0" Foreground="#FFCBD5E1" Text="WPF migration of your file browser, scan history, rename, organize, and preview workflow." />
                </StackPanel>

                <Border Grid.Column="1" Padding="14,10" CornerRadius="12" Background="#FF172554">
                    <StackPanel>
                        <TextBlock Foreground="#FF93C5FD" FontWeight="SemiBold" Text="Current folder" />
                        <TextBlock x:Name="SelectedPathTextBlock" Width="360" TextTrimming="CharacterEllipsis" Foreground="White" Text="No folder selected" />
                    </StackPanel>
                </Border>
            </Grid>
        </Border>

        <Border Grid.Row="1" Margin="0,16,0,16" Style="{StaticResource PanelBorderStyle}">
            <StackPanel Orientation="Horizontal"
                VerticalAlignment="Center">

                <Button Margin="0,0,8,0" Click="SelectFolder_Click">Select folder</Button>
                <Button Margin="0,0,8,0" Click="Scan_Click">Scan</Button>
                <Button Margin="0,0,8,0" Click="Organize_Click">Organize</Button>
                <Button Margin="0,0,8,0" Click="History_Click">History</Button>

                <Button Style="{StaticResource SecondaryButtonStyle}"
                Margin="0,0,20,0"
                Click="Refresh_Click">
                    Refresh
                </Button>

                <TextBlock VerticalAlignment="Center"
                   Margin="0,0,10,0"
                   Foreground="{StaticResource MutedBrush}"
                   Text="Filter" />

                <ComboBox x:Name="FilterComboBox"
                  Width="180"
                  VerticalAlignment="Center"
                  SelectionChanged="FilterComboBox_SelectionChanged" />

            </StackPanel>
        </Border>

        <Grid Grid.Row="2">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="3*" />
                <ColumnDefinition Width="20" />
                <ColumnDefinition Width="2*" />
            </Grid.ColumnDefinitions>

            <Border Grid.Column="0" Style="{StaticResource PanelBorderStyle}">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="*" />
                    </Grid.RowDefinitions>

                    <TextBlock FontSize="18" FontWeight="SemiBold" Text="Files" />
                    <DataGrid x:Name="FilesGrid"
                              Grid.Row="1"
                              Margin="0,16,0,0"
                              AutoGenerateColumns="False"
                              IsReadOnly="True"
                              SelectionMode="Extended"
                              SelectionUnit="FullRow"
                              HeadersVisibility="Column"
                              GridLinesVisibility="None"
                              CanUserResizeRows="False"
                              CanUserReorderColumns="False"
                              CanUserResizeColumns="True"
                              ScrollViewer.HorizontalScrollBarVisibility="Auto"
                              ScrollViewer.VerticalScrollBarVisibility="Auto"
                              SelectionChanged="FilesGrid_SelectionChanged">
                        
                        <DataGrid.ContextMenu>
                            <ContextMenu>

                                <MenuItem Header="Rename" Click="RenameSelected_Click"/>

                                <MenuItem Header="Move" Click="MoveSelected_Click"/>

                                <MenuItem Header="Delete" Click="DeleteSelected_Click"/>

                            </ContextMenu>
                        </DataGrid.ContextMenu>
                        
                        <DataGrid.Resources>
                            
                            <Style TargetType="DataGridRow">
                                <Setter Property="Height" Value="34"/>
                            </Style>

                            <Style TargetType="TextBlock">
                                <Setter Property="TextTrimming" Value="CharacterEllipsis"/>
                                <Setter Property="VerticalAlignment" Value="Center"/>
                            </Style>
                        </DataGrid.Resources>

                        <DataGrid.Columns>

                            <DataGridTextColumn Header="Name"
                            Binding="{Binding Name}"
                            Width="2*" />

                            <DataGridTextColumn Header="Type"
                            Binding="{Binding Extension}"
                            Width="80" />

                            <DataGridTextColumn Header="Size"
                            Binding="{Binding Size, StringFormat={}{0:N0} B}"
                            Width="110" />

                            <DataGridTextColumn Header="Modified"
                            Binding="{Binding ModifiedDate, StringFormat={}{0:yyyy-MM-dd HH:mm}}"
                            Width="160" />

                            
                        </DataGrid.Columns>
                    </DataGrid>
                    
                </Grid>
            </Border>

            <Border Grid.Column="2" Style="{StaticResource PanelBorderStyle}">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="*" />
                    </Grid.RowDefinitions>

                    <TextBlock FontSize="18" FontWeight="SemiBold" Text="Preview" />
                    <ContentControl x:Name="PreviewContent" Grid.Row="1" Margin="0,16,0,0" />
                </Grid>
            </Border>
        </Grid>

        <Border Grid.Row="3" Margin="0,16,0,0" Style="{StaticResource PanelBorderStyle}">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="220" />
                </Grid.ColumnDefinitions>

                <TextBlock x:Name="TotalTextBlock" VerticalAlignment="Center" Text="Total: 0" />
                <TextBlock x:Name="SelectedTextBlock" Grid.Column="1" Margin="20,0,0,0" VerticalAlignment="Center" Text="Selected: 0" />
                <TextBlock x:Name="ProgressTextBlock" Grid.Column="2" Margin="20,0,0,0" VerticalAlignment="Center" Foreground="{StaticResource MutedBrush}" Text="Ready" />
                <ProgressBar x:Name="ScanProgressBar" Grid.Column="3" Height="18" Minimum="0" Visibility="Collapsed" />
            </Grid>
        </Border>
    </Grid>
</Window>
