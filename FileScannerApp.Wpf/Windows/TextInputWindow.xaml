<Window x:Class="FileScannerApp.Wpf.TextInputWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Rename file"
        Height="190"
        Width="460"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize">
    <Border Margin="18" Style="{StaticResource PanelBorderStyle}">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>

            <TextBlock x:Name="PromptTextBlock" FontSize="16" FontWeight="SemiBold" />
            <TextBlock Grid.Row="1" Margin="0,6,0,12" Foreground="{StaticResource MutedBrush}" Text="Enter the new file name." />
            <TextBox x:Name="ValueTextBox" Grid.Row="2" Height="36" VerticalContentAlignment="Center" />

            <StackPanel Grid.Row="3" Margin="0,16,0,0" Orientation="Horizontal" HorizontalAlignment="Right">
                <Button Style="{StaticResource SecondaryButtonStyle}" Click="Cancel_Click">Cancel</Button>
                <Button Margin="8,0,0,0" Click="Ok_Click">Save</Button>
            </StackPanel>
        </Grid>
    </Border>
</Window>
